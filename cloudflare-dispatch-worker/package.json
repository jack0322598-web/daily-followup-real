{
  "name": "cloudflare-dispatch-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test:scheduled": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "wrangler": "^4.22.0"
  }
}
